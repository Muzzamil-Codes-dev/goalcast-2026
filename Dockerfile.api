FROM python:3.11-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1 PIP_NO_CACHE_DIR=1

COPY requirements.txt pyproject.toml ./
RUN pip install -r requirements.txt
COPY src ./src
RUN pip install -e .

COPY . .
EXPOSE 8000
CMD ["uvicorn", "goalcast.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
